# Backend

Python + FastAPI application — auth, business rules, REST API (`/ipos`, `/chat`, `/watchlist`, ...), and the
retrieval-augmented answer flow that talks to the database and AI engine.

Built out starting in a later epic.
