# Copy to .env (and `source` it, or use python-dotenv) for local dev. Never commit the real .env.
# Use the Supabase "Transaction pooler" connection string (Connect button in the dashboard) —
# scraper.py is a short-lived job, not a persistent server.
DATABASE_URL=
