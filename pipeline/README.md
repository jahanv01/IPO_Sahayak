# Pipeline

DRHP scraper and parser scripts — pulls prospectus documents from SEBI (and NSE/BSE for calendar data),
extracts text, and chunks it for the AI engine's document store.

Built out starting in a later epic.
