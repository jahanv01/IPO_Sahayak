name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI skeleton
        run: echo "CI pipeline skeleton — backend/frontend test & lint jobs added in a later epic."
