# Frontend

React.js web app — the screens users see and interact with (chat, IPO browsing, watchlist, etc).

Talks to the backend only through the REST API defined in `/backend`. Built out starting in a later epic.
