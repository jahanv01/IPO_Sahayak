# Frontend

React.js web app (built with Vite) — the screens users see and interact with (chat, IPO browsing,
watchlist, etc). Talks to the backend only through the REST API defined in `/backend`.

## Local dev

```
npm install
npm run dev
```

Set `VITE_API_URL` (see `.env.example`) to point at your local or deployed backend.
